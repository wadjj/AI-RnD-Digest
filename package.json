{
  "name": "ai-rnd-digest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-feed.js",
    "generate:force": "node scripts/generate-feed.js --force",
    "generate:preview": "node scripts/generate-feed.js --ignore-state",
    "prepare": "node scripts/prepare-digest.js",
    "sync:folo-token": "node scripts/sync-folo-token.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
